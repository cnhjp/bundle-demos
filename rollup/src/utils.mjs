export function add(a, b) {
  return a + b;
}

export function addPI(a) {
  return a + Math.PI;
}

export function multiply(a, b) {
  return a * b;
}

module.exports = {
  add,
  addPI,
  multiply,
};
